@model SisRestaurante.Web.Models.RestauranteViewModel

@{
    ViewBag.Title = "Delete";
}

<h2>Apagar Restaurante</h2>

<h3>Tem certeza que deseja apagar esse restaurante?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Nome)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Nome)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                @Html.ActionLink("Cancelar", "Index", new { @area = "" }, new { @class = "btn btn-danger" })
                <button type="submit" value="" class="btn btn-success">Excluir</button> 
            </div>
        </div>
    }
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}